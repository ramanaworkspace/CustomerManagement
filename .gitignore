# Build output
bin/
obj/
publish/
*.zip

# User-specific files
*.user
*.suo
*.userosscache
*.sln.docstates

# Logs
*.log

# AWS Lambda tooling
aws-lambda-tools-defaults.json

# Secrets
secrets.json
*.secrets.json

# IDE files
.vscode/
.idea/
*.swp

# OS generated files
.DS_Store
Thumbs.db

# JetBrains Rider
.idea/

# Test results
TestResult*/  
*.ncrunch*  
*.vstest*

# dotnet watch / hot reload
.dotnet-watch.json

# Local .terraform directories
.terraform/
**/.terraform/
.providers/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Optional: ignore graph output files generated by `terraform graph`
# *.dot

# Optional: ignore plan files saved before destroying Terraform configuration
# Uncomment the line below if you want to ignore planout files.
# planout

# GitHub Actions artifacts
*.coverage
coverage/
/.vs/AWSLambdaPractice01/CopilotIndices/17.14.878.3237/*.db
/.vs/AWSLambdaPractice01/FileContentIndex/*.vsidx
/.vs/AWSLambdaPractice01/config/*.config
/.vs/AWSLambdaPractice01/v17/*.v2
/.vs/AWSLambdaPractice01/v17/*.json
/.vs/ProjectEvaluation/*.bin
/.vs/AWSLambdaPractice01/CopilotIndices/17.14.878.3237/*.db-shm
/.vs/AWSLambdaPractice01/CopilotIndices/17.14.878.3237/*.db-wal
/src/CustomerManagement.API/.vs/CustomerManagement/CopilotIndices/17.14.878.3237/*.db
/src/CustomerManagement.API/.vs/CustomerManagement/CopilotIndices/17.14.878.3237/*.db-shm
/src/CustomerManagement.API/.vs/CustomerManagement/CopilotIndices/17.14.878.3237/*.db-wal
/src/CustomerManagement.API/.vs/CustomerManagement/DesignTimeBuild/*.v2
/src/CustomerManagement.API/.vs/CustomerManagement/FileContentIndex/*.vsidx
/src/CustomerManagement.API/.vs/CustomerManagement/config/*.config
/src/CustomerManagement.API/.vs/CustomerManagement/v17/*.json
